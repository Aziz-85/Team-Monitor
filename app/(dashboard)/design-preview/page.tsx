import { DesignPreviewClient } from './DesignPreviewClient';

export const metadata = {
  title: 'Design Preview | Team Monitor',
};

export default function DesignPreviewPage() {
  return <DesignPreviewClient />;
}
